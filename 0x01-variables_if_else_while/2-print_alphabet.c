#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * print the alphabet in lowercase
 *
 * return: Always 0 (Success)
 */
int main(void)
{
int ch;
for (ch = 'a'; ch <= '2'; ch++)
{
putchart(ch);
}

putchart('\n');

return (0);
}
