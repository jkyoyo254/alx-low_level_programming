#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * print the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int ch;
for (ch = 'a'; ch <= '2'; ch++)
{
putchar(ch);
}

putchar('\n');

return (0);
}
